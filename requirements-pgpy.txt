# AWS SDK (essential only)
boto3>=1.26.0
botocore>=1.29.0

# PGPy for PGP operations (included in package)
pgpy>=0.6.0

# Cryptography and dependencies (required by PGPy)
cryptography>=41.0.0
cffi>=1.15.0
pycparser>=2.21
